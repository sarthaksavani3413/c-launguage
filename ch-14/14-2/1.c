#include <stdio.h>

main()
{
    int size, len;

    printf("name : ");
    scanf("%s", &size);

    int a[size];

    for (int i = 0; i < size; i++)
    {
        len = a[size];
    }

    printf("length : %d ", len);
}